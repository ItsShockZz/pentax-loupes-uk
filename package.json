{
  "name": "pentax-loupes-uk",
  "version": "1.0.0",
  "private": true,
  "description": "PENTAX Loupes UK — static site plus the loupe passport (Vercel Functions in api/).",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "test": "node --test \"tests/*.test.mjs\""
  }
}
